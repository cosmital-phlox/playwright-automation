# Dependencies
node_modules/

# Playwright output (regenerated every run — not source)
playwright-report/
test-results/
blob-report/
playwright/.cache/
results.json

# Saved login sessions — secrets. CI restores these from the
# FRONTEND_STORAGE_STATE secret; never commit them.
playwright/.auth/

# Load-test run outputs (regenerated every run — keep the .jmx plans + docs, not results)
jmeter.log
load-testing/*.jtl
load-testing/**/*.jtl
load-testing/report-*/
load-testing/pv923/
load-testing/staging/

# OS junk
.DS_Store
